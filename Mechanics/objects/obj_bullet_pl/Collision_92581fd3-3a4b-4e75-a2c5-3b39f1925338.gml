instance_destroy();

obj_main.pontos += 10;

with (other)
{
	//vidas -= 10;
	
// Quando o inimigo levar tiro, ele vai ficar andando para escapar dos tiros
	interfer_demands = true;
	shooting_demand ++
}